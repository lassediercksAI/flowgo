# Sanity check fixture

Some intro text before the map.

```flowgo
box a 0 0 Start
box b 240 0 Finish
edge a b
```

An unrelated code block that must be left untouched:

```js
console.log("not a flowgo block");
```

An empty flowgo block:

```flowgo
```

Text after everything.
